//
//  SOZIPStatusDynamicIsland_Leading.swift
//  SOZIP
//
//  Created by Changjin Ha on 2023/04/05.
//

import SwiftUI
import WidgetKit
import ActivityKit

struct SOZIPStatusDynamicIsland_Leading: View {
    var body: some View {
        Image(uiImage: UIImage(named : "ic_liveActivities") ?? UIImage())
            .clipShape(RoundedRectangle(cornerRadius: 15))
    }
}
